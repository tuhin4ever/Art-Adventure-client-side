import { useLoaderData } from "react-router-dom";


const Instructors = () => {
    const data = useLoaderData( )
    console.log(data)
    return (
        <div>
            
        </div>
    );
};

export default Instructors;